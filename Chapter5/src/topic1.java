/*Loops and Files*/
public class topic1 {
	public String print()
	{
		return "1. ++ and -- are operators that add and subtract one fom their operands.\n"
				+ "2. Compare to Postfix and Prefix:\n"
				+ "E.g. int x=1, y; y=x++;  It assigns the value of x to the variable y. Then, the variable x is incremented.\n"
				+ "E.g. int x=1, y; y=++x;  The variable x is incremented. Then, it assigns the value of x to the variable y.\n"
				+ "3. A loop is a part of a program that repeats.\n"
				+ "4. A While loop is known as a pretest loop, which means it tests its expression before each iteration.\n"
				+ "5. The do-while loop is postest loop, which means its boolean expression is tested after each iteration.\n"
				+ "6. The for loop is ideal for perforaming a known number of iterations.\n"
				+ "7. A loop that is inside another loop is called a nested loop.\n"
				+ "8. The break statment causes a loop to terminate early.\n"
				+ "9. The continue statment causes a loop to stop its current iteration and begin the nest one.\n"
				+ "10. PrintWriter Class and FileWriter class to write data to a file and reading data from a file.\n";
	}
}
