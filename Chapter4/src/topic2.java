/*Comparing String Objects*/
/*The Conditional Operator*/
public class topic2 {
	public String Print()
	{
		return "1. You should not use relational operators to compare String objects. Instead you should use an appropriate String method.\n"
				+ "2. str1.euqals(str2); It returns true if the two strings are equal, or false if they are not queal.\n"
				+ "3. str1.compareTo(str2); It returns -1 if str1 is less than str2. It returns +1 if str1 is greater than str2. It returns 0 if they are equal.\n"
				+ "4. BooleanExpression ? Value1 ; Value2;  If (BooleanExrepssion) {Value1} else {Value2}.\n"
				+ "5. The switch statement lets the value of a variable or expression determine where the program will branch to.\n"
				+ "6. Swithch (Expression) {Case value_1: {statement; break;} ....Case value_n: {statement; break;} default: {statement; break;} }\n"
				+ "";
	}
	
}
