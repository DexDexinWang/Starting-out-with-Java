/*More about Passing Argument*/
/*Instance Fields and Methods*/
public class topic2 
{
	public String print()
	{
		return "1. A method can have multiple parameter variables, allowing you to pass multiple arguments to the method.\n"
				+ "2. Passed by Value. In Java, all arguments of the primitive data types are passed by value, which means that a copy of an argument`s value is passed into a parameter variable.\n"
				+ "3. If a parameter variable is changed inside a method, it has no effect on the original argument. Because a method`s parameter variables are separate and distinct from the arguments that are listed inside the paretheses of a method call.\n"
				+ "4. Each Instance of a class has its own set of filds, whcih are known as instance fields.\n";
	}
}
