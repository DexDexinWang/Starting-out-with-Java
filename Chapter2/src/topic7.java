/*Creating Named Constants with final*/
public class topic7 {
	public String print()
	{
		return "1. Constants with final:\n"
				+ "final double INTEREST_RATE=0.069;\n"
				+ "A named constant is a vairable whose value is read only and cannot be changed during the program`s execution.\n"
				+ "2. The Math.PI named constant, which is part of the Java API. It provides a predefined named constant, Math.PI. \"area= Math.PI * radius * radius\"\n";
	}
}
